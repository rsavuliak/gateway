version: '3'
services:
  gateway:
    build: .
    container_name: gateway
    ports:
      - "80:80"
      - "443:443"
    networks:
      - gateway-network

networks:
  gateway-network:
    external: true